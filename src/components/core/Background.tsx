import React from 'react';
import { Image } from 'react-native';

const Background = () => {
  return <Image source={require('assets/images/background.png')} className="size-full" />;
};

export default Background;
